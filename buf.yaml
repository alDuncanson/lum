# buf configuration (v2). `buf` compiles our .proto files without a
# protoc install and runs lint checks against them. Run `nix develop -c buf generate`.
version: v2
modules:
  - path: proto
lint:
  use:
    - STANDARD
breaking:
  use:
    - FILE
